class AppMetrics {
  AppMetrics._();

  // Shared toolbar height for all app bars
  static const double toolbarHeight = 94.0;

  // Bottom (banner/controls) heights per screen
  static const double mainBottomHeight = 136.0; // banner + trend switch
  static const double photosBottomHeight = 44.0; // banner only
}
